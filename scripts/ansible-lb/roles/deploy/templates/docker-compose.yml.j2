version: '3.8'

services:
  haproxy:
    image: haproxy:2.8-alpine
    container_name: haproxy
    ports:
      - "8443:8443"
      - "8444:8444"
    volumes:
      - {{ haproxy_cfg_file_path }}:/usr/local/etc/haproxy/haproxy.cfg:ro
      - {{ haproxy_certs_dir }}:/usr/local/etc/haproxy/certs:ro
